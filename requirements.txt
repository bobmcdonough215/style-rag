# Style RAG — Python Dependencies
# Install with: pip install -r requirements.txt
#
# After first successful run, freeze exact versions with:
# pip freeze > requirements.txt
#
# Note: sqlite3 is part of Python's standard library — do not add it here.

# ─── LangChain Core ───────────────────────────────────────────────────────────
# LangChain split into multiple packages in 2024 — all are required
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-chroma>=0.1.0
langchain-ollama>=0.2.0
langchain-text-splitters>=0.3.0

# ─── Vector Store ─────────────────────────────────────────────────────────────
chromadb>=0.5.0

# ─── UI ───────────────────────────────────────────────────────────────────────
streamlit>=1.40.0

# ─── Document Loading ─────────────────────────────────────────────────────────
pypdf>=4.0.0
beautifulsoup4>=4.12.0

# ─── Environment & Utilities ──────────────────────────────────────────────────
python-dotenv>=1.0.0
httpx>=0.27.0
numpy>=1.26.0
